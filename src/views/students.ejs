<%- include("partials/_header") %>

<div class="container mt-8">
  <div class="row">
    <div class="col-md-10">
      <table class="table table-dark table-bordered table-hover">
        <thead>
          <tr>
            <td>n°</td>
            <td>Nombre</td>
            <td>Direccion</td>
            <td>Telefono</td>
            <td>Acciones</td>
          </tr>
        </thead>
        <tbody>
          <% if (students) { %> <% for(var i = 0; i < students.length; i++) {
          %>
          <tr>
            <td><%= (i + 1) %></td>
            <td><%= students[i].name %></td>
            <td><%= students[i].address %></td>
            <td><%= students[i].phone %></td>
            <td class="d-flex gap-2">
              <a href="/update/<%= students[i].id %>" class="btn btn-info">
                Editar
              </a>
              <a href="/delete/<%= students[i].id %>" class="btn btn-danger">
                Eliminar
              </a>
            </td>
          </tr>
          <% } %> <% } %>
        </tbody>
      </table>
    </div>
    <div class="col-md-5">
      <div class="card-body">
        <form action="/add" method="POST">
          <input
            type="text"
            name="name"
            placeholder="Name"
            class="form-control mb-2"
            autofocus
          />
          <input
            type="text"
            name="address"
            placeholder="Address"
            class="form-control mb-2"
          />
          <input
            type="text"
            name="phone"
            placeholder="Phone"
            class="form-control mb-2"
          />

          <button type="submit" class="btn btn-info">Guardar estudiante</button>
        </form>
      </div>
    </div>
  </div>
</div>

<%- include("partials/_footer") %>
